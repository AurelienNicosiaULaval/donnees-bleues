---
title: "Données bleues"
subtitle: "Des données québécoises pour enseigner la statistique et la science des données"
toc: false
page-layout: full
---

```{r}
#| label: home-page-data
#| include: false
library(dplyr)
library(readr)
library(stringr)

catalogue_path <- "data/metadata/catalogue.csv"
activity_catalogue_path <- "data/metadata/catalogue_activites.csv"

catalogue <- if (file.exists(catalogue_path)) {
  read_csv(catalogue_path, show_col_types = FALSE)
} else {
  tibble::tibble()
}

activities <- if (file.exists(activity_catalogue_path)) {
  read_csv(activity_catalogue_path, show_col_types = FALSE)
} else {
  tibble::tibble()
}

n_datasets <- nrow(catalogue)
n_themes <- if (n_datasets > 0) length(unique(catalogue$theme)) else 0
n_activities <- nrow(activities)
n_contexts <- if (n_activities > 0) length(unique(activities$context)) else 0

theme_list <- if (n_datasets > 0) {
  catalogue |>
    count(theme, sort = TRUE) |>
    slice_head(n = 6) |>
    pull(theme) |>
    paste(collapse = ", ")
} else {
  "Je ne sais pas."
}

home_escape <- function(value) {
  value <- as.character(value)
  value[is.na(value)] <- ""
  value <- gsub("&", "&amp;", value, fixed = TRUE)
  value <- gsub("<", "&lt;", value, fixed = TRUE)
  value <- gsub(">", "&gt;", value, fixed = TRUE)
  value <- gsub('"', "&quot;", value, fixed = TRUE)
  value <- gsub("'", "&#39;", value, fixed = TRUE)
  value
}

featured_ids <- c(
  "bixi",
  "qualite-air-horaire",
  "budgets-municipaux-quebec"
)

featured_datasets <- if (n_datasets > 0) {
  catalogue |>
    filter(id %in% featured_ids) |>
    mutate(feature_order = match(id, featured_ids)) |>
    arrange(feature_order)
} else {
  tibble::tibble()
}

render_featured_datasets <- function(data) {
  if (!is.data.frame(data) || nrow(data) == 0L) {
    return('<p class="home-empty">Aucune fiche à mettre en avant pour le moment.</p>')
  }

  paste(vapply(seq_len(nrow(data)), function(i) {
    row <- data[i, , drop = FALSE]
    image <- file.path("assets", "cards", paste0(row$id, ".jpg"))
    href <- str_replace(row$fiche, "[.]qmd$", ".html")
    score <- if (!is.na(row$zero_waste_score)) {
      paste0(row$zero_waste_score, " / 30")
    } else {
      "Je ne sais pas."
    }

    paste0(
      '<article class="home-dataset-card">',
      '<img src="', home_escape(image), '" alt="Illustration de ', home_escape(row$short_title), '">',
      '<div class="home-dataset-card-body">',
      '<span>', home_escape(row$theme), '</span>',
      '<h3>', home_escape(row$short_title), '</h3>',
      '<p>', home_escape(row$title), '</p>',
      '<div class="home-dataset-card-meta">',
      '<strong>', home_escape(score), '</strong>',
      '<a href="', home_escape(href), '">Ouvrir la fiche</a>',
      '</div>',
      '</div>',
      '</article>'
    )
  }, character(1)), collapse = "\n")
}

activity_link <- function(activity) {
  if (!is.data.frame(activity) || nrow(activity) == 0L) {
    return(NULL)
  }

  href <- str_replace(activity$activity_url[[1]], "[.]qmd$", ".html")
  title <- activity$title[[1]]
  question <- activity$question[[1]]
  list(
    href = href,
    title = title,
    question = question
  )
}

render_example_step <- function(number, label, title, text, href) {
  paste0(
    '<a class="home-example-step" href="', home_escape(href), '">',
    '<span>', home_escape(number), '</span>',
    '<strong>', home_escape(label), '</strong>',
    '<em>', home_escape(title), '</em>',
    '<small>', home_escape(text), '</small>',
    '</a>'
  )
}

render_home_example <- function(catalogue, activities, target_dataset_id = "bixi") {
  if (!is.data.frame(catalogue) || nrow(catalogue) == 0L) {
    return('<p class="home-empty">Aucun exemple complet disponible pour le moment.</p>')
  }

  dataset <- catalogue |>
    filter(.data$id == target_dataset_id) |>
    slice_head(n = 1)

  if (nrow(dataset) == 0L) {
    return('<p class="home-empty">Aucun exemple complet disponible pour le moment.</p>')
  }

  dataset_href <- str_replace(dataset$fiche[[1]], "[.]qmd$", ".html")

  short_activity <- activities |>
    filter(.data$dataset_id == target_dataset_id, .data$duration == "30-45 minutes") |>
    slice_head(n = 1) |>
    activity_link()

  long_activity <- activities |>
    filter(.data$dataset_id == target_dataset_id, .data$duration == "2-3 heures") |>
    slice_head(n = 1) |>
    activity_link()

  short_step <- if (is.null(short_activity)) {
    ""
  } else {
    render_example_step(
      "02",
      "Activité courte",
      short_activity$title,
      short_activity$question,
      short_activity$href
    )
  }

  long_step <- if (is.null(long_activity)) {
    ""
  } else {
    render_example_step(
      "03",
      "Activité longue",
      long_activity$title,
      long_activity$question,
      long_activity$href
    )
  }

  paste0(
    '<article class="home-example-card">',
    '<div class="home-example-copy">',
    '<span>Exemple en 2 minutes</span>',
    '<h3>Partir de ', home_escape(dataset$short_title[[1]]), ' et arriver à une activité prête à adapter.</h3>',
    '<p>Cette séquence montre le geste central du site : choisir une source, comprendre son contexte, puis la transformer en situation d’enseignement.</p>',
    '</div>',
    '<div class="home-example-steps">',
    render_example_step(
      "01",
      "Fiche",
      dataset$short_title[[1]],
      "Source, unité statistique, variables, limites et résultats R.",
      dataset_href
    ),
    short_step,
    long_step,
    '</div>',
    '</article>'
  )
}
```

::: {.home-page}

::: {.home-hero}
::: {.home-hero-copy}

::: {.home-kicker}
Infrastructure pédagogique ouverte
:::

# Données bleues

::: {.home-subtitle}
Des jeux de données québécois pour apprendre à importer, nettoyer, visualiser, modéliser et surtout interpréter avec contexte.
:::

::: {.home-lede}
Beaucoup d'exemples pédagogiques en statistique, en programmation R et en science des données reposent sur des jeux de données non québécois, souvent américains. Ils peuvent être utiles, mais ils laissent souvent le territoire, les institutions, les licences, les unités statistiques et les limites loin de la salle de classe. Données bleues propose une autre entrée : des sources québécoises et canadiennes vérifiées, transformées en fiches, activités et pistes de projet.
:::

::: {.home-hero-actions}
[Explorer le catalogue](catalogue.qmd){.dataset-button}
[Commencer par une activité](activites.qmd){.dataset-button .secondary}
:::

:::

::: {.home-hero-signal}
::: {.home-signal-label}
La promesse
:::

Passer d'un fichier trouvé en ligne à une ressource de cours complète : source, contexte, variables, limites, activité courte, activité longue et discussion pédagogique.
:::

:::

::: {.home-stat-strip}
::: {.home-stat}
`r n_datasets`

fiches de jeux de données
:::

::: {.home-stat}
`r n_themes`

thèmes pédagogiques
:::

::: {.home-stat}
`r n_activities`

activités prêtes à adapter
:::

::: {.home-stat}
`r n_contexts`

contextes d'enseignement
:::
:::

::: {.home-section .home-thesis}
::: {.home-section-heading}
::: {.home-label}
Point de départ
:::

## Le contexte n'est pas une décoration. C'est une partie de la méthode.

Un jeu de données enseigne toujours plus que des colonnes. Il enseigne une façon de mesurer, une institution, une géographie, une licence, des choix de collecte, des angles morts et des limites d'interprétation.
:::

::: {.home-thesis-grid}
::: {.home-thesis-copy}
Quand les exemples viennent presque toujours d'ailleurs, le geste statistique devient trop abstrait. On peut apprendre une fonction R ou un modèle, mais perdre la question la plus formatrice : qu'est-ce que ces données représentent réellement?

Données bleues remet ce contexte au centre. Le site aide à choisir des données locales, à les documenter correctement et à en faire du matériel pédagogique réutilisable plutôt qu'un exemple jetable.
:::

::: {.home-thesis-panel}
::: {.home-panel-title}
Au lieu de demander seulement :
:::

« Quel modèle appliquer? »

::: {.home-panel-title}
On apprend aussi à demander :
:::

« Qui a produit ces données, pour quel usage, avec quelles limites, et que peut-on raisonnablement conclure? »
:::
:::
:::

::: {.home-section .home-promise}
::: {.home-section-heading}
::: {.home-label}
Ce que le site apporte
:::

## Une source devient une séquence d'apprentissage.

Chaque fiche vise à réduire le temps de préparation tout en augmentant la qualité du contexte présenté aux personnes étudiantes.
:::

::: {.home-promise-grid}
::: {.home-promise-item}
::: {.home-promise-number}
01
:::

### Trouver

Repérer rapidement une source québécoise par thème, niveau, concepts enseignables ou score zéro déchet.
:::

::: {.home-promise-item}
::: {.home-promise-number}
02
:::

### Comprendre

Identifier l'unité statistique, les variables principales, la provenance, la licence et les limites.
:::

::: {.home-promise-item}
::: {.home-promise-number}
03
:::

### Enseigner

Transformer la source en activité courte, laboratoire, mini-projet ou rapport reproductible.
:::

::: {.home-promise-item}
::: {.home-promise-number}
04
:::

### Réutiliser

Reprendre le même jeu de données dans plusieurs apprentissages : nettoyage, visualisation, inférence, modélisation, communication.
:::
:::
:::

::: {.home-section .home-paths}
::: {.home-section-heading}
::: {.home-label}
Par où commencer?
:::

## Trois portes d'entrée, selon le besoin du moment.

Le site peut servir de catalogue de sources, de banque d'activités ou de cadre d'évaluation pédagogique.
:::

::: {.home-path-grid}
::: {.home-path-card}
::: {.home-path-eyebrow}
01 / Catalogue
:::

### Je cherche une source

Comparer les fiches par thème, niveau, concepts, format et potentiel pédagogique.

[Ouvrir le catalogue](catalogue.qmd)
:::

::: {.home-path-card}
::: {.home-path-eyebrow}
02 / Activités
:::

### Je prépare une séance

Choisir une activité courte, une tâche longue ou un mini-projet déjà relié à une source.

[Voir les activités](activites.qmd)
:::

::: {.home-path-card}
::: {.home-path-eyebrow}
03 / Évaluation
:::

### J'évalue un jeu de données

Utiliser la grille zéro déchet pour décider si une source mérite d'être préparée.

[Lire le cadre](zero-waste.qmd)
:::
:::
:::

::: {.home-section .home-example}
```{r}
#| echo: false
#| results: asis
cat(render_home_example(catalogue, activities, "bixi"))
```
:::

::: {.home-section .home-featured}
::: {.home-section-heading}
::: {.home-label}
Exemples
:::

## Trois fiches qui montrent l'esprit du projet.

Ces fiches couvrent des réalités proches de la classe : mobilité, environnement, finances municipales et données publiques documentées.
:::

::: {.home-dataset-grid}
```{r}
#| echo: false
#| results: asis
cat(render_featured_datasets(featured_datasets))
```
:::
:::

::: {.home-section .home-current}
::: {.home-current-panel}
::: {.home-current-copy}
::: {.home-label}
Catalogue actuel
:::

## Une base locale qui grandit.

Cette version contient `r n_datasets` fiches réparties dans `r n_themes` thèmes pédagogiques. Les thèmes les plus visibles dans le catalogue actuel sont : `r theme_list`.

Le projet couvre notamment l'eau, l'environnement, la mobilité, l'éducation, la démographie, les services publics, le territoire et les données institutionnelles. Les sources et leurs dates d'accès sont listées dans les références.
:::

::: {.home-current-actions}
[Consulter les sources](references.qmd){.dataset-button .secondary}
[À propos du projet](about.qmd){.dataset-button}
:::
:::
:::

::: {.home-final}
::: {.home-final-copy}
::: {.home-label}
Finalité
:::

## Enseigner avec des données qui ont une adresse.

Données bleues avance par sources vérifiées, activités testables, scripts reproductibles et limites explicites. L'objectif est simple : rendre les exemples de statistique et de science des données plus situés, plus réutilisables et plus honnêtes.
:::

::: {.home-final-actions}
[Contribuer](contribuer.qmd){.dataset-button}
:::
:::

:::
